<!DOCTYPE html>
<html>
<head>
    <title>Windows Updates Report</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 20px;
            background-color: #f3f3f3;
            color: #333;
        }
        h1 {
            color: #2b579a;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 20px;
            background-color: #fff;
        }
        th, td {
            border: 1px solid #e1e1e1;
            padding: 12px 15px;
            text-align: left;
        }
        th {
            background-color: #2b579a;
            color: white;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #eef7ff;
        }
    </style>
</head>
<body>
    <h1>Windows Updates Report</h1>
    <table>
        <tr>
            <th>Server Name</th>
            <th>Required Updates</th>
        </tr>
        <tr>
            <td>{{ inventory_hostname }}</td>
            <td>
                {% if updates | length == 0 %}
                    <div class="no-updates">No updates needed</div>
                {% else %}
                <ul>
                    {% for update_id, update in updates.items() %}
                        {% if update.categories is defined %}
                        <li>
                            <strong>KB{{ update.kb[0] }}:</strong> {{ update.title }} 
                            <br> Categories: {{ update.categories | join(', ') }}
                        </li>
                        {% else %}
                        <li>
                            <strong>KB{{ update.kb[0] }}:</strong> {{ update.title }} 
                            <br> Categories: Not Available
                        </li>
                        {% endif %}
                    {% endfor %}
                </ul>
                {% endif %}
            </td>
        </tr>
    </table>
</body>
</html>
